{
  "name": "functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "echo \"(js) nothing to build\"",
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions,firestore",
    "shell": "firebase functions:shell",
    "start": "npm run serve",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^9.36.0",
    "globals": "^15.11.0"
  }
}
